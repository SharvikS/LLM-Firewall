[pytest]
# Unit tests live in tests/. The repo-root test_ml_pipeline.py is a manual
# end-to-end script (makes live Groq calls) — run it directly with
# `python test_ml_pipeline.py`, not under pytest.
testpaths = tests
